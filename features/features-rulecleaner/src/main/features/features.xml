<?xml version="1.0" encoding="UTF-8"?>
<features xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" name="rulecleaner-${project.version}"
          xmlns="http://karaf.apache.org/xmlns/features/v1.2.0"
          xsi:schemaLocation="http://karaf.apache.org/xmlns/features/v1.2.0 http://karaf.apache.org/xmlns/features/v1.2.0">
    <repository>mvn:org.opendaylight.openflowplugin/features-openflowplugin/${openflow.plugin.version}/xml/features
    </repository>
    <repository>mvn:org.opendaylight.netconf/features-restconf/${restconf.version}/xml/features</repository>
    <repository>mvn:org.opendaylight.dluxapps/features-dluxapps/${dluxapps.version}/xml/features</repository>
    <repository>mvn:org.opendaylight.l2switch/features-l2switch/${l2switch.version}/xml/features</repository>

    <feature name='falcon-rulecleaner-ui' description="Falcon :: RuleCleaner :: UI" version='${project.version}'>
        <feature version="${restconf.version}">odl-restconf</feature>
        <feature version="${restconf.version}">odl-mdsal-apidocs</feature>
        <feature version="${dluxapps.version}">odl-dluxapps-topology</feature>
        <feature version="${dluxapps.version}">odl-dluxapps-yangui</feature>
        <feature version="${l2switch.version}">odl-l2switch-switch</feature>
        <bundle>mvn:org.falcon.rulecleaner.securestate/securestate-impl/{{VERSION}}</bundle>
        <bundle>mvn:org.falcon.rulecleaner.statemonitor/statemonitor-impl/{{VERSION}}</bundle>
        <bundle>mvn:org.falcon.rulecleaner.rulechecker/rulechecker-impl/{{VERSION}}</bundle>
        <bundle>mvn:org.falcon.rulecleaner.cleanup/cleanup-impl/{{VERSION}}</bundle>
        <bundle>mvn:org.falcon.rulecleaner.cleanuphandler/cleanuphandler-impl/{{VERSION}}</bundle>
    </feature>
</features>
